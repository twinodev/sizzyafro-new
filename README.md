# SizzyAfro — Next.js + Tailwind + Firebase starter

## Setup

1. Copy `.env.example` to `.env.local` and fill your Firebase config values (use `NEXT_PUBLIC_` prefixed keys).

2. Install dependencies:

```bash
npm install
```

3. Run the development server:

```bash
npm run dev
```

Open http://localhost:3000
